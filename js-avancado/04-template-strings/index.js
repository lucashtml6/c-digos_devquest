let nome = "Lucas";

console.log(`Olá, ${nome}, seja bem vindo(a).`);

const mensagem = nome => `Olá ${nome}, seja bem vindo(a)`;

console.log(`${mensagem(nome)} Estamos felizes em vê-lo(a) novamente!`);